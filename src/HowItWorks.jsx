import React from 'react';
import './HowItWorks.css'; // CSS file for styling this component

const HowItWorks = () => {
  return (
    <section className="how-it-works">
      <h2>How it works?</h2>
      <div className="steps">
        {/* Each step with an image and description */}
      </div>
    </section>
  );
};

export default HowItWorks;
